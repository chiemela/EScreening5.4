<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eliamtechnologies.escreening">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartScreeningActivity"
            android:label="@string/title_activity_start_screening"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".ValidIDActivity" />
        <activity android:name=".TranscriptsActivity" />
        <activity android:name=".TestimonialActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".PersonalStatementActivity" />
        <activity android:name=".PassportActivity" />
        <activity android:name=".IELTSActivity" />
        <activity android:name=".CVActivity" />
        <activity android:name=".BirthCertificateActivity" />
        <activity android:name=".StartScreeningActivity2" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Login" />
        <activity android:name=".Register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>